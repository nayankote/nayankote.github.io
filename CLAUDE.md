# Claude Code Instructions

## Git Workflow
- Always run `git pull --rebase` before committing to avoid push conflicts
- GitHub Actions may commit to the repo (e.g., notes processing), so local branch can fall behind
